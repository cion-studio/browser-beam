{
  "name": "browser-beam",
  "version": "1.8.1",
  "description": "Lightweight wrapper around fetch API, with better error handling and more powerful config options.",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc -p . && cp package.json dist && cp README.md dist",
    "deploy": "yarn version && npm run build && cd dist && npm publish",
    "test": "ts-node ./tests/server.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/node-fetch": "^2.5.12",
    "@typescript-eslint/eslint-plugin": "^5.19.0",
    "@typescript-eslint/parser": "^5.19.0",
    "eslint": "^7.30.0",
    "node-fetch": "^2.6.1",
    "typescript": "^4.3.5"
  }
}
